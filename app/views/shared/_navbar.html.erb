      <header class="header navbar navbar-expand-lg position-absolute navbar-sticky">
        <div class="container px-3">
          <%= link_to (user_signed_in? ? authenticated_root_path : unauthenticated_root_path), class: "navbar-brand pe-3" do %>
            <%= t('navigation.navbar.brand') %>
          <% end %>

          <div class="d-flex align-items-center ms-auto">
            <!-- Language Switcher -->
            <div class="dropdown me-3">
              <a href="#" class="nav-link d-flex align-items-center" data-bs-toggle="dropdown" aria-expanded="false">
                <i class="bi bi-globe me-1"></i>
                <%= I18n.locale == :es ? 'ES' : 'EN' %>
                <i class="bi bi-chevron-down ms-1 small"></i>
              </a>
              <ul class="dropdown-menu dropdown-menu-end">
                <li>
                  <%= link_to set_locale_path(new_locale: 'es'), class: "dropdown-item #{'active' if I18n.locale == :es}" do %>
                    <span class="me-2">🇪🇸</span> Español
                  <% end %>
                </li>
                <li>
                  <%= link_to set_locale_path(new_locale: 'en'), class: "dropdown-item #{'active' if I18n.locale == :en}" do %>
                    <span class="me-2">🇬🇧</span> English
                  <% end %>
                </li>
              </ul>
            </div>

            <% if user_signed_in? %>
              <div class="dropdown">
                <a href="#" class="nav-link text-decoration-none d-flex align-items-center" data-bs-toggle="dropdown" aria-expanded="false">
                  <i class="bi bi-person-circle fs-4 me-2"></i>
                  <%= t('navigation.navbar.greeting', email: current_user.email) %>
                  <i class="bi bi-chevron-down ms-1"></i>
                </a>
                <ul class="dropdown-menu dropdown-menu-end">
                  <% if current_user.admin? %>
                    <li><%= link_to t('navigation.navbar.manage_influencers'), influencers_path, class: "dropdown-item" %></li>
                    <li><hr class="dropdown-divider"></li>
                  <% end %>
                  <li><%= link_to t('navigation.navbar.logout'), destroy_user_session_path, data: { "turbo-method": :delete }, class: "dropdown-item" %></li>
                </ul>
              </div>
            <% else %>
              <%= link_to t('navigation.navbar.login'), new_user_session_path, class: "btn btn-primary btn-sm fs-sm rounded" %>
            <% end %>
          </div>
        </div>
      </header>
